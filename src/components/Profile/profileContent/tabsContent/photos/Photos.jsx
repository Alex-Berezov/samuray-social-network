import React from 'react';
import classes from './Photos.module.css';

const Photos = () => {
    return (
        <div className={`${classes.tab} ${classes.tab_4}`}>Under construction</div>
    );
}

export default Photos;