import React from 'react';
import classes from './Settings.module.css';

const Settings = () => {
    return (
        <section className="settings_part">
            <h2>Settings</h2>
        </section>
    );
}

export default Settings;